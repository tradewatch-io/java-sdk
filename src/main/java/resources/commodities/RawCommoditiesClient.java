/**
 * This file was auto-generated by Fern from our API Definition.
 */

package io.tradewatch.client.resources.commodities;

import com.fasterxml.jackson.core.JsonProcessingException;
import io.tradewatch.client.core.ClientOptions;
import io.tradewatch.client.core.ObjectMappers;
import io.tradewatch.client.core.QueryStringMapper;
import io.tradewatch.client.core.RequestOptions;
import io.tradewatch.client.core.TradewatchApiApiException;
import io.tradewatch.client.core.TradewatchApiException;
import io.tradewatch.client.core.TradewatchApiHttpResponse;
import io.tradewatch.client.errors.UnprocessableEntityError;
import io.tradewatch.client.resources.commodities.requests.CommoditiesGetInsightsRequest;
import io.tradewatch.client.resources.commodities.requests.CommoditiesGetQuoteRequest;
import io.tradewatch.client.resources.commodities.requests.CommoditiesGetQuotesRequest;
import io.tradewatch.client.resources.commodities.requests.CommoditiesGetSymbolsRequest;
import io.tradewatch.client.types.CommodityTypesList;
import io.tradewatch.client.types.CursorPageTCustomizedNewsOut;
import io.tradewatch.client.types.CursorPageTCustomizedSymbolsOutFull;
import io.tradewatch.client.types.LastQuote;
import io.tradewatch.client.types.LastQuotes;
import java.io.IOException;
import java.lang.Object;
import java.lang.String;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawCommoditiesClient {
  protected final ClientOptions clientOptions;

  public RawCommoditiesClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Get the last quote tick for the provided symbols.
   */
  public TradewatchApiHttpResponse<LastQuotes> getQuotes(CommoditiesGetQuotesRequest request) {
    return getQuotes(request,null);
  }

  /**
   * Get the last quote tick for the provided symbols.
   */
  public TradewatchApiHttpResponse<LastQuotes> getQuotes(CommoditiesGetQuotesRequest request,
      RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("commodities/quotes");QueryStringMapper.addQueryParameter(httpUrl, "symbols", request.getSymbols(), false);
      if (request.getPrecision().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "precision", request.getPrecision().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new TradewatchApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, LastQuotes.class), response);
        }
        try {
          if (response.code() == 422) {
            throw new UnprocessableEntityError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
          }
        }
        catch (JsonProcessingException ignored) {
          // unable to map error response, throwing generic error
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new TradewatchApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new TradewatchApiException("Network error executing HTTP request", e);
      }
    }

    /**
     * Get the last quote tick for the provided symbol.
     */
    public TradewatchApiHttpResponse<LastQuote> getQuote(CommoditiesGetQuoteRequest request) {
      return getQuote(request,null);
    }

    /**
     * Get the last quote tick for the provided symbol.
     */
    public TradewatchApiHttpResponse<LastQuote> getQuote(CommoditiesGetQuoteRequest request,
        RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("commodities/quote");QueryStringMapper.addQueryParameter(httpUrl, "symbol", request.getSymbol(), false);
        if (request.getPrecision().isPresent()) {
          QueryStringMapper.addQueryParameter(httpUrl, "precision", request.getPrecision().get(), false);
        }
        if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("GET", null)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new TradewatchApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, LastQuote.class), response);
          }
          try {
            if (response.code() == 422) {
              throw new UnprocessableEntityError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            }
          }
          catch (JsonProcessingException ignored) {
            // unable to map error response, throwing generic error
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new TradewatchApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (IOException e) {
          throw new TradewatchApiException("Network error executing HTTP request", e);
        }
      }

      /**
       * Get list of available symbols
       */
      public TradewatchApiHttpResponse<CursorPageTCustomizedSymbolsOutFull> getSymbols() {
        return getSymbols(CommoditiesGetSymbolsRequest.builder().build());
      }

      /**
       * Get list of available symbols
       */
      public TradewatchApiHttpResponse<CursorPageTCustomizedSymbolsOutFull> getSymbols(
          RequestOptions requestOptions) {
        return getSymbols(CommoditiesGetSymbolsRequest.builder().build(),requestOptions);
      }

      /**
       * Get list of available symbols
       */
      public TradewatchApiHttpResponse<CursorPageTCustomizedSymbolsOutFull> getSymbols(
          CommoditiesGetSymbolsRequest request) {
        return getSymbols(request,null);
      }

      /**
       * Get list of available symbols
       */
      public TradewatchApiHttpResponse<CursorPageTCustomizedSymbolsOutFull> getSymbols(
          CommoditiesGetSymbolsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("commodities/symbols");if (request.getSize().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "size", request.getSize().get(), false);
          }
          if (request.getMode().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "mode", request.getMode().get(), false);
          }
          if (request.getType().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "type", request.getType().get(), false);
          }
          if (request.getCountry().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "country", request.getCountry().get(), false);
          }
          if (request.getCursor().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "cursor", request.getCursor().get(), false);
          }
          if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              return new TradewatchApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CursorPageTCustomizedSymbolsOutFull.class), response);
            }
            try {
              if (response.code() == 422) {
                throw new UnprocessableEntityError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new TradewatchApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (IOException e) {
            throw new TradewatchApiException("Network error executing HTTP request", e);
          }
        }

        /**
         * Get recent commodities insights.
         */
        public TradewatchApiHttpResponse<CursorPageTCustomizedNewsOut> getInsights() {
          return getInsights(CommoditiesGetInsightsRequest.builder().build());
        }

        /**
         * Get recent commodities insights.
         */
        public TradewatchApiHttpResponse<CursorPageTCustomizedNewsOut> getInsights(
            RequestOptions requestOptions) {
          return getInsights(CommoditiesGetInsightsRequest.builder().build(),requestOptions);
        }

        /**
         * Get recent commodities insights.
         */
        public TradewatchApiHttpResponse<CursorPageTCustomizedNewsOut> getInsights(
            CommoditiesGetInsightsRequest request) {
          return getInsights(request,null);
        }

        /**
         * Get recent commodities insights.
         */
        public TradewatchApiHttpResponse<CursorPageTCustomizedNewsOut> getInsights(
            CommoditiesGetInsightsRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("commodities/insights");if (request.getCursor().isPresent()) {
              QueryStringMapper.addQueryParameter(httpUrl, "cursor", request.getCursor().get(), false);
            }
            if (request.getSize().isPresent()) {
              QueryStringMapper.addQueryParameter(httpUrl, "size", request.getSize().get(), false);
            }
            if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            Request.Builder _requestBuilder = new Request.Builder()
              .url(httpUrl.build())
              .method("GET", null)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Accept", "application/json");
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            try (Response response = client.newCall(okhttpRequest).execute()) {
              ResponseBody responseBody = response.body();
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                return new TradewatchApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CursorPageTCustomizedNewsOut.class), response);
              }
              try {
                if (response.code() == 422) {
                  throw new UnprocessableEntityError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new TradewatchApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (IOException e) {
              throw new TradewatchApiException("Network error executing HTTP request", e);
            }
          }

          /**
           * Get list of available commodity types
           */
          public TradewatchApiHttpResponse<CommodityTypesList> getTypes() {
            return getTypes(null);
          }

          /**
           * Get list of available commodity types
           */
          public TradewatchApiHttpResponse<CommodityTypesList> getTypes(
              RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

              .addPathSegments("commodities/types");if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              try (Response response = client.newCall(okhttpRequest).execute()) {
                ResponseBody responseBody = response.body();
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  return new TradewatchApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CommodityTypesList.class), response);
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                throw new TradewatchApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
              }
              catch (IOException e) {
                throw new TradewatchApiException("Network error executing HTTP request", e);
              }
            }
          }
