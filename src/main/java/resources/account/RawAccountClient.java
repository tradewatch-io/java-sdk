/**
 * This file was auto-generated by Fern from our API Definition.
 */

package io.tradewatch.client.resources.account;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import io.tradewatch.client.core.ClientOptions;
import io.tradewatch.client.core.ObjectMappers;
import io.tradewatch.client.core.QueryStringMapper;
import io.tradewatch.client.core.RequestOptions;
import io.tradewatch.client.core.TradewatchApiApiException;
import io.tradewatch.client.core.TradewatchApiException;
import io.tradewatch.client.core.TradewatchApiHttpResponse;
import io.tradewatch.client.errors.UnprocessableEntityError;
import io.tradewatch.client.resources.account.requests.AccountGetUsageRequest;
import java.io.IOException;
import java.lang.Object;
import java.lang.String;
import java.util.Map;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawAccountClient {
  protected final ClientOptions clientOptions;

  public RawAccountClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Get the usage statistics of your API account
   */
  public TradewatchApiHttpResponse<Map<String, Object>> getUsage() {
    return getUsage(AccountGetUsageRequest.builder().build());
  }

  /**
   * Get the usage statistics of your API account
   */
  public TradewatchApiHttpResponse<Map<String, Object>> getUsage(RequestOptions requestOptions) {
    return getUsage(AccountGetUsageRequest.builder().build(),requestOptions);
  }

  /**
   * Get the usage statistics of your API account
   */
  public TradewatchApiHttpResponse<Map<String, Object>> getUsage(AccountGetUsageRequest request) {
    return getUsage(request,null);
  }

  /**
   * Get the usage statistics of your API account
   */
  public TradewatchApiHttpResponse<Map<String, Object>> getUsage(AccountGetUsageRequest request,
      RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("usage");if (request.getLimit().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "limit", request.getLimit().get(), false);
      }
      if (request.getInterval().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "interval", request.getInterval().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new TradewatchApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, new TypeReference<Map<String, Object>>() {}), response);
        }
        try {
          if (response.code() == 422) {
            throw new UnprocessableEntityError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
          }
        }
        catch (JsonProcessingException ignored) {
          // unable to map error response, throwing generic error
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new TradewatchApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new TradewatchApiException("Network error executing HTTP request", e);
      }
    }
  }
